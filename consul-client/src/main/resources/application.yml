spring:
  application:
    name: eureka-client
  cloud:
    consul:
      host: localhost
      port: 8500
#      discovery:
#        service-name: service-XXX
server:
  port: 2001
  
      